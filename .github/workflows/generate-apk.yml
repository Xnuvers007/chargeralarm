name: Generate APK and Release

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v3
      with:
        gradle-version: '8.9'

    - name: Build Release APK
      run: gradle assembleRelease

    - name: Generate Release Tag
      id: tag
      run: echo "release_tag=v1.0.${{ github.run_number }}" >> $GITHUB_ENV

    - name: Rename APK
      run: mv app/build/outputs/apk/release/app-release.apk ChargerAlarm-${{ env.release_tag }}.apk

    - name: Upload APK as Artifact
      uses: actions/upload-artifact@v4
      with:
        name: ChargerAlarm-APK
        path: ChargerAlarm-${{ env.release_tag }}.apk


    - name: Create Release and Upload APK
      uses: softprops/action-gh-release@v2
      with:
        tag_name: ${{ env.release_tag }}
        name: Release ${{ env.release_tag }}
        files: ChargerAlarm-${{ env.release_tag }}.apk
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
